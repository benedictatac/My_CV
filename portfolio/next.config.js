// next.config.mjs
const nextConfig = {
    reactStrictMode: true,  // Enables React Strict Mode for additional checks and warnings
    eslint: {
      ignoreDuringBuilds: true  // Temporarily ignores ESLint during builds
    }
  };
  
  export default nextConfig;
  